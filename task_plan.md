# 转录方案产品化计划

1. [完成] 将首次 `setup` 改为明确选择云端、本地或不转录，并提供非交互参数。
2. [完成] 实现可选的本地 Whisper 转录，安装和模型下载必须经过明确选择。
3. [完成] 以官方价格页补齐百炼的价格、估算和账单边界说明。
4. [完成] 补齐收藏/喜欢的防重兼容与测试。
5. [完成] 新用户默认直连百炼 `qwen3-asr-flash`，不再依赖第三方 `douyin-mcp`。
6. [完成] 增加无凭据、无下载、无付费调用的本机转录能力发现，并明确 MiniMax 当前仅发现 TTS 时不可用。
7. [完成] 更新使用文档、补全回归测试并进行干净安装验证。
8. [完成] 增加本地媒体留存保护：临时文件回收、单视频下载上限、每日百炼额度与失败重试。
9. [记录，未实现] 评论精华应作为独立的显式可选能力：复用已授权 Playwright 会话抓取单视频评论，不依赖第三方 `douyin-mcp`。实现时必须限制每视频评论页数和总数、保留评论原链接/作者/时间作为出处、去重并只写入摘要与可验证引用；不得采集他人私密内容或把评论全量无筛选写入知识库。
10. [记录] ClawHub 当前 `Current version`、Versions 的 Latest 标记和默认安装内容存在索引不一致；GitHub Release 与 Gitee 正式标签是权威发布来源。在 ClawHub 修复 Latest 指针前不继续上传新版本。
11. [部分实现] 飞书输出保持可选、默认本地 Markdown。`configure-feishu --mode webhook` 已配置内置 webhook 通知器，webhook 仅从 `FEISHU_WEBHOOK_URL` 环境变量读取且通知失败不阻断本地同步；`bitable-existing`/`bitable-new` 已在知识库生成推荐字段模板与授权提示。多维表 App API 直写、链接解析、字段映射和测试写入尚未实现，必须在用户完成飞书登录、应用授权和表格共享后再做；不得复用开发者 WorkBuddy/个人腾讯云或飞书凭据。
12. [记录，待验证] 腾讯云录音文件识别 API 文档支持音频 URL/MP4（URL 时长 <= 5 小时、文件 <= 1 GB），通用版本每月 10 小时免费；需以用户自己的腾讯云凭据和抖音授权播放 URL 完成端到端验证后，才作为公开可选 provider。
13. [产品决策，待实现] 腾讯云通用录音文件识别验证成功后，改为公开 Skill 的默认推荐 provider：用户首次注册腾讯云、开通 ASR，并将自己的 SecretId/SecretKey 保存至系统钥匙串或环境变量；不得写进项目配置。把“每月 10 小时免费”表述为日常个人使用的长期低门槛层，不承诺终身免费或免费额度永不变化。百炼保留为备选，Whisper 保留为离线方案。
14. [部分实现] Obsidian 已产品化为 `configure-obsidian --vault ... --subdir ...`：自动切换知识库目录、创建收藏/喜欢/日报/模板/系统目录、两套 Markdown 模板、日报索引、飞书字段模板和临时写入检查。飞书后续仍需完成已有表/新建表三档的 App API 直写与权限诊断；无飞书或权限不足始终不阻断本地同步。
16. [完成，未发布] 已从本机日报提取稳定的可选分析结构：内容梳理、价值判断、深度分析、延展补充、行动启示、关联知识。公开笔记分为“要点 / 原始材料 / 研判 / 来源”：默认 Obsidian 模板仅保留原始材料与来源，避免未分析笔记出现空栏目；analysis adapter 仅能以受限对象写入非空研判字段，分析关闭时不会插入这些栏目。新版首次生成笔记使用中文分组标题，既有 ledger 与旧笔记不会被自动重写。日报的跨条目洞察继续属于 `daily`，不复制到单条笔记。
15. [分段验证完成，真实媒体全链路待可观测复验] 2026-07-30 用户完成开通后，已用 WorkBuddy 现有腾讯云 SecretId/SecretKey 提交一段临时生成的中文语音至 `CreateRecTask`，并成功轮询至完成，验证了鉴权、录音文件识别服务、`16k_zh` 引擎和异步任务链路。腾讯无法直接拉取一条已授权抖音临时播放 URL，故不能承诺“ASR 直连抖音零下载”。已独立验证：抖音媒体带正确浏览器请求头可流式读取；COS 可写入并删除临时对象；腾讯可从短时 COS 签名 URL 成功识别短音频，前缀对账为 0。组合后的真实抖音媒体异步终态没有可靠回传，故“抖音流 -> COS -> ASR”尚待带脱敏结果文件的可观测复验，不得提前写为成功。该候选路径不写本地视频文件，但需要用户自己的 COS 桶、区域和最短生命周期/删除失败回收策略，不能再把腾讯云配置描述为只需一个 ASR Key。官方定价页（2026-07-20 更新）确认通用录音文件识别每月 10 小时免费，只计算识别成功的音频时长，失败调用不计费；大模型引擎无此免费额，后付费默认关闭的用户应保持关闭。公开 collector 的页面脚本转义错误也已修复，已确认既有 Skill profile 为 authenticated、收藏接口返回 `status_code=0`，无需用户再次扫码。


14. [2026-08-05 完成] 2.2 SiliconFlow 为抖音默认 ASR；百炼降为可选；local_whisper 补 Referer。
